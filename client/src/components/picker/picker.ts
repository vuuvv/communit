import { Component, Input, Output, ViewEncapsulation, HostBinding, EventEmitter } from '@angular/core';

@Component({
  selector: 'v-picker',
  templateUrl: './picker.html',
  styleUrls: ['./picker.less'],
  encapsulation: ViewEncapsulation.None,
})
export class PickerComponent {
}
